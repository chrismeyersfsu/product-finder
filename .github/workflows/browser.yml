name: browser

# Thin trigger: the logic lives in packages/browser/ci.sh so CI is runnable
# and debuggable locally. List a package's own tree, its workspace
# dependencies, and the lockfile in `paths`.
on:
  push:
    paths:
      - "packages/browser/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/browser.yml"
  pull_request:
    paths:
      - "packages/browser/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/browser.yml"

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: ./packages/browser/ci.sh
