name: sites

# Thin trigger: the logic lives in packages/sites/ci.sh so CI is runnable
# and debuggable locally. List a package's own tree, its workspace
# dependencies, and the lockfile in `paths`.
on:
  push:
    paths:
      - "packages/sites/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/sites.yml"
  pull_request:
    paths:
      - "packages/sites/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/sites.yml"

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: ./packages/sites/ci.sh
